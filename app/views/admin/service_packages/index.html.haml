%section#records= render 'records'
%aside#actions= render 'actions'
= render '/shared/admin/make_sortable', :tree => false if !searching? && Admin::ServicePackagesController.sortable? && Service::Package.count > 1
